package org.hiero.otter.fixtures.network;

/**
 * Interface representing a mesh network topology.
 *
 * <p>This interface extends the {@link Topology} interface and is used to define a mesh network topology,
 * where each node is connected to all other nodes in the network.
 */
public interface MeshTopology extends Topology {}
